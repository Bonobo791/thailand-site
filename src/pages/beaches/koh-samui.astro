---
import Layout from '../../layouts/Layout.astro';
import { getCollection, type CollectionEntry } from 'astro:content';

// Get the Koh Samui page from the content collection
const allPages = await getCollection('pages');
const kohSamuiPage = allPages.find((page: CollectionEntry<'pages'>) => page.slug === 'beaches/koh-samui');

if (!kohSamuiPage) {
    throw new Error('Koh Samui page not found in content collection');
}

// Get the content directly
const { Content } = await kohSamuiPage.render();
---

<Layout title={kohSamuiPage.data.title || 'Koh Samui Travel Guide'}>
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-6">{kohSamuiPage.data.title || 'Koh Samui Travel Guide'}</h1>

        <p class="mb-6">
            {kohSamuiPage.data.description || "Everything you need to know about visiting Koh Samui, Thailand's second-largest island"}
        </p>

        <div class="markdown-content">
            <Content />
        </div>
    </div>
</Layout>
